const Discord = require("discord.js");
const ms = require('ms')

module.exports = {
    name: "mute",
    aliases: [],
    category: "moderation",
    description: "Mutes the user for the specified time",
    run: async(bot, message, args, g) => {

    }
}
